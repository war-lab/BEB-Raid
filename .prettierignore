node_modules/
dist/
dev-dist/
package-lock.json

# 設計ドキュメント（正本）と既存Markdownは整形対象にしない（差分ノイズ防止）
*.md

# ビジュアル刷新の方向性モックアップ（docs/20の参照物。凍結された参考資料のため整形しない）
docs/design/

# Playwright MCP の作業ファイル
.playwright-mcp/
*.png
*.woff2
*.mp3

# コンテンツパイプライン（CLI）の生成物。手で編集する正本ではないため整形対象外
# （T-32でJSON.stringifyそのままの出力とPrettierの配列整形が食い違うため除外）
content/
