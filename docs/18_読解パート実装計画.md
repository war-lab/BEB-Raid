# 18. 読解パート（Part6/7）実装計画（自走タスクシート）

## 1. 位置づけと使い方

- 全量レビューで判明した「読解（Part6/7）が型だけ予約され実装・コンテンツともに存在しない」穴を埋めるための、**AIコーディングエージェントが1タスク=1セッションで完遂できる粒度**の作業指示書。
- 設計判断の正本は [ADR 0006](adr/0006-読解パートの完成方針.md)（読解をセッション長で階層化する折衷案）。02・03の読解関連記述（配分・「Part7はソロ専用」等）が上流正本であり、本書はそれを実装可能な水準に具体化する。矛盾したら ADR 0006 と 02・03 が優先し本書を直す。
- 各タスクシートは「読むべき正本の節」「作成・変更するファイル」「実装手順」「機械検証可能な完了条件」「停止条件」を持つ。**シートと3節（事前決定事項）に無い設計判断が必要になったら、推測で進めず停止して人間に確認する**こと。
- 自走度の凡例は [10](10_F4-F6実装計画.md) の1節と同じ（🟢 AI単独完遂可 / 🟡 人間チェックポイント付き / 🔴 人間主体）。
- セッション開始プロンプトの例:

  ```
  docs/18_読解パート実装計画.md の T-xxx を実装してください。
  着手前に同書の2節（共通ルール）・3節（事前決定事項）・ADR 0006 と、シート記載の正本参照節を必ず読むこと。
  作業は task/T-xxx-説明 ブランチで行い、dev への PR（ドラフト）作成までがタスクです。
  完了時は2節の完了手順（検証・STATUS.md更新）まで実施してください。
  ```

## 2. 全タスク共通ルール（ガードレール）

**[13の2節](13_M2実装計画.md)（上流は [10の2節](10_F4-F6実装計画.md)）を全面的に継承する**。本計画での差分は以下。

### 2.1 ブランチ運用

- `task/T-xxx-説明` ブランチで作業し、dev への **PR（ドラフト作成→CI通過→マージ）** で入れる。dev直コミットはしない。
- 着手時に `git fetch` して**最新の dev から分岐**する。同一作業ツリーで別セッションが作業中なら `git worktree` で分離する。
- 契約変更（T-103）は09の2節どおり**共有面変更のみの単独PR**とする。

### 2.2 新規依存の方針

- **新規依存ゼロ**を原則とする。読解はテキストのみで、音声・キャッシュの新規抽象を要しない。必要になったら停止して人間に確認する。

### 2.3 不変条件（毎タスクで回帰確認する）

1. **オフライン正常系**: 解答記録は IndexedDB へ即時保存（04）。読解も通常の attempt として記録する。
2. **attempts追記のみ**（削除APIを作らない）。
3. **プライバシー境界**: 読解の questionStats も既存の匿名集計（deviceTokenと結合しない）に乗せるだけ。新たな個人紐づき送信を作らない（04）。
4. **縮退設計**: 共有API無効時にソロ学習の動作・表示が変わらないこと。読解は共有APIに依存しない機能なので、API無効でも完全に動くこと。
5. **platform層に触れない**: 読解はテキストのみのため、音声再生・キャッシュ抽象（platform/）を変更しない。

### 2.4 停止規則

- シート・3節・ADR 0006 に無い設計判断が必要になったら停止する。
- 既存の audio_set / SRS / レート / 中断復帰 / タグ統計の再利用で不整合が出たら、勝手に回避せず停止して報告する。
- 完了条件を1つでも満たせない場合、条件を緩めず停止して報告する。

## 3. 事前決定事項（正文）

数値・命名はすべて本節が正本（変更する場合は本節を直す）。

### 3.1 スキーマ（ADR 0006 判断3）

`packages/shared-schema/src/types.ts` に以下を追加する。

```ts
/** 読解の刺激文書。Part7複数パッセージは配列長2〜3 */
export interface Passage {
  id: string
  /** email | notice | article | chat | form | advertisement 等。表示ラベルと出題文脈 */
  kind: string
  /** 本文。Part6は空所マーカー [[1]]…[[4]] を埋め込む */
  text: string
}
```

`Question` に `passages?: Passage[] | null` を追加する（`text_passage` 用）。

- 設問は既存の `subQuestions: SubQuestion[]` を再利用する（音声セットと同一構造）。
- **Part6**: `passages` 1件、`text` に `[[1]]…[[4]]` を4個埋め込む。`subQuestions` は4問で、n番目が `[[n+1]]`（1起点マーカー）に対応する。選択肢4つ。文挿入問題もこの枠（選択肢が挿入文）で表現する。
- **Part7単一**: `passages` 1件、`subQuestions` 2〜4問。
- **Part7複数**: `passages` 2〜3件、`subQuestions` 5問。相互参照が必要な設問には `subQuestion` にタグ（例: `cross-reference`）を付ける。
- 追加フィールドは後方互換のため `SCHEMA_VERSION` は据え置く（2のまま）。
- `validate.ts` に `text_passage` の必須検証を追加する: `part` が 6 または 7、`passages.length >= 1`、`subQuestions.length >= 1`、`keyVocab.length >= 1`（vocab_card以外の既存規約）、Part6 は空所マーカー数と `subQuestions.length` が一致すること。

### 3.2 採点（ADR 0006 判断4）

- 読解は `computeSetResult`（2/3ルール）を**使わない**。各 subQuestion を独立した採点対象とし、1問ごとに正誤を記録・レート更新する。
- レート更新対象: 読解の全 subQuestion を **Rセクションレート**に反映する（`engine/rating.ts` の既存Elo。L/R区分はRとして数える）。SRS復習・語彙カードがレート対象外なのは従来どおり。

### 3.3 セッション統合（ADR 0006 判断2）

`engine/quickPack.ts` / `engine/curriculum.ts` の配分に組み込む。

| パック | 読解 |
|--------|------|
| 3分 | なし（SRS復習のみ。現状維持） |
| 7分 | Part6 ＋ Part7単一を弱点配分に追加 |
| 15分 | ＋ Part7単一を厚めに。複数パッセージは入れない |
| 「じっくり読解」モード（新規） | Part7複数パッセージ専用（着席・自宅想定）。1〜2セットを通しで。時間目安を提示 |

フェーズ配分（03の1.2）への反映:

- P1（〜550）: Part6を少量導入（Part5文法の延長）。Part7複数は出さない。
- P2（550〜650）: Part7単一を本格投入（既存「Part7シングル15%」枠に対応）。
- P3（650〜）: 「じっくり読解」でPart7複数（既存「Part7マルチ30%」枠に対応）。

### 3.4 学習ロジック統合（ADR 0006 判断6・7）

- **弱点タグ**: 読解用の解法タグ（先読み/スキャン/パラフレーズ照合/相互参照/推論/語彙推測）を `tagStats` の集計対象に含める。既存のタグ体系（03の7.1「解法」分類）に乗せる。正答率60%未満で弱点判定・出題重み増（既存ロジックを流用）。
- **SRS再出題**: 本文まるごとの再出題はしない。誤答→keyVocab を SRS へ、弱点タグの重みを増やす（既存の 03の3.2 循環に接続）。再挑戦は同一タグ・keyVocab の別パッセージ・別Part5類題で行う。
- **noEarphoneFilter**: 読解コンテンツ投入後、`engine/noEarphoneFilter.ts` がリスニングを text_passage/text_blank に差し替える設計が本来どおり機能することを確認する（現状は text_passage の実体が無く text_blank にしか差し替わっていない）。

### 3.5 UI（ADR 0006 判断2）

- **新規 `screens/ReadingScreen.tsx`** を作る（DrillScreen に分岐追加ではなく専用画面。本文＋設問の2ペインが既存4択UIと別レイアウトのため）。
- 単一パッセージ: 上に本文（スクロール可）、下に設問＋選択肢。片手UIの下半分に解答ボタンを置く既存規約（02の2.2）を維持。
- 複数パッセージ: 本文をタブ切替（文書1/文書2/文書3）。相互参照の存在を示唆しすぎない範囲で表示。
- Part6: 本文中の空所を `___(1)___` 形式で表示し、該当設問へジャンプ。選択後に本文の空所へ反映。
- **中断復帰**: 既存のセッション中断復帰（append-only attempts・activeSession）に乗せ、1問解答ごとに保存する既存方式を使う。セット途中の離脱でも復帰可能にする。
- **ペース表示**: リスニングの自動誤答タイマー（15秒）は付けない。目標ペースの目安（本試験RCは約1分/問）を柔らかく提示する。時間切れで解き切れない層の底上げが目的で、速答を煽るものにしない。

### 3.6 コンテンツと初期在庫（ADR 0006 判断5）

- `packages/cli` に読解パック生成を追加する。本文＋設問の整合（複数パッセージで参照先が実在するか、Part6のマーカー数と設問数の一致）を機械検証する。
- **読解は人手レビュー必須ゲートを最初から有効化**する。AIクロスレビューのみでの配信を認めない。`origin` にレビュー実施状況を正直に記録する。
- 初期在庫の目標量（暫定・ドッグフード実測で調整）: **Part6 30セット（120問）／Part7単一 40セット（約120問）／Part7複数 15セット（75問）**。MVP（フェーズR-1）ではPart6＋Part7単一を先行し、複数はR-2で追加する。

## 4. タスクシート

次のタスクIDは T-103 以降（T-102まで消化済み）。判断IDは J-51 以降。

### フェーズR-1（通勤内で完結する読解: Part6・Part7単一）

**T-103 ✅ 完了（2026-07-21・Opus実装）スキーマ拡張（共有面・単独PR）**
- 正本参照: 3.1節、04の2節、ADR 0006 判断3
- 変更: `packages/shared-schema/src/types.ts`（`Passage`・`Question.passages`・`SubQuestion.tags`）、`validate.ts`（text_passage を passages+subQuestions 構造へ再定義・Part6マーカー整合）、`validate.test.ts`（新規14件）
- 完了条件: `text_passage`（Part6/7）の妥当・不正パックの検証テストが通る。既存の shared-schema テスト全通過。04の2節に `passages`/`SubQuestion.tags`/読解検証を追記。`npm run lint`・`npm run build`・`npm test` 全通過。**すべて達成**（shared-schema 61件・全ワークスペース通過。04の2節更新済み）。

**T-104 ✅ 完了（2026-07-21）ReadingScreen（単一パッセージ＋Part6）**
- 正本参照: 3.5節、02の2.2節
- 変更: `screens/ReadingScreen.tsx`（新規）、`components/PassageText.tsx`（新規。本文表示＋Part6空所ジャンプ）、`engine/subQuestionLookup.ts`（新規。DrillScreenのaudio_set用ヘルパを共有化）、`ScreenName` への追加、App.tsx の画面分岐
- 完了条件: **すべて達成**。Part6（4空所・非線形ジャンプ）・Part7単一（複数subQuestions）を解ける。中断復帰（既存activeSessionに乗せた形での動作確認）・ペース表示（15秒タイマー無し・柔らかい経過秒数表示）が動く。単体テスト5件全通過。
- **未着手事項（意図的）**: HomeScreenからの導線・7分/15分パック内での画面切替方式はT-105（セッション配分への組込）の設計判断のため本タスクでは実装しない（詳細はSTATUS.md参照）。
- **未検証事項**: 実ブラウザでの目視確認は本環境にブラウザ自動化ツールが無く未実施（詳細はSTATUS.md参照）。

**T-105 ✅ 完了（2026-07-23）セッション配分への組込**
- 正本参照: 3.3節、03の1.2節、engine/quickPack.ts・curriculum.ts
- 変更: `engine/quickPack.ts`・`engine/curriculum.ts`（7分/15分パックにPart6・Part7単一を配分。フェーズP1/P2の配分反映）
- 完了条件: 7分・15分パックに読解が「なぜ出たか」ラベル付きで混ざる。複数パッセージは通常パックに入らないことをテストで担保。全通過。**すべて達成**。
- **配分実装**: `drillCategoryOf`（M1）・`resolveM2Category`（M2）に`reading`カテゴリを追加。`isReadingAllocatable(question)`ヘルパ（`passages.length<2`）で単一/複数を判定し、**Part7複数（passages 2件以上）は弱点タグ・フェーズを問わず常にnullを返し通常パックから除外**（weakness判定より前段で除外するためP3のweaknessバケットにも流れ込まない）。「なぜ出たか」ラベルは既存のweakTag/keyVocabReview/allocation機構（カテゴリ非依存）にそのまま乗るため追加実装不要。
- **配分数値（quickPackConfig.json・curriculumConfig.json。すべて暫定・ドッグフード実測で調整）**: M1（quickPackConfig.json、フェーズ未確定時のフォールバック）はP1相当としてvocabから0.1移しreading=0.1を追加（vocab0.4/part2 0.25/part5 0.25/reading0.1）。P1テンプレは同じ値。P2テンプレは03の1.2節の原表（語彙25/Part3・4先読み35/Part5・6 25/Part7シングル15）をJ-9振替前の形へ復元（vocab0.25/listening0.35/part5 0.25/reading0.15）。**P3テンプレにはreadingバケットを追加していない**（3.3節はP3のPart7複数を「じっくり読解」モード=T-108/T-109専用と規定しており、通常パックへの新規導入の記述が無いための解釈。P3のPart6/Part7単一が通常パックに出したい場合は別途判断が必要＝要確認事項として残す）。
- **「15分はPart7単一を厚めに」の解釈**: durationごとに配分%を変える仕組みは既存アーキテクチャに無い（allocation%はフェーズのみに依存し、durationはtotalItemsのみを決める）。新規のduration別%テーブルはシートに明記が無い設計判断のため追加せず、**既存のtotalItemsスケーリングで絶対数が自然に増える**（15分40問×reading15%>7分20問×reading15%）ことで「厚めに」を満たすと解釈した。
- **HomeScreen導線・7分/15分パック内での画面切替方式（T-104で保留）を本タスクで確定**: `DrillScreen`と`ReadingScreen`はどちらもセッション状態（`useSessionStore`）を共有しているため、各画面に「現在itemの`question.format`を見て担当外ならもう一方へ`navigate()`する」対の`useEffect`を追加した。`DrillScreen`は`format==='text_passage'`ならreading画面へ、`ReadingScreen`は`format!=='text_passage'`ならdrill画面へ切り替える（advanceSessionは呼ばずitem自体は進めない。screen切替のみ）。これにより7分/15分パックへの読解混在後もHomeScreen「今日のクエスト」・RaidScreenのレイド挑戦セッションから追加のUI分岐なしで読解itemに到達できる。
- **未解決の副次課題（意図的に対象外）**: RaidScreen起点のレイドセッション（`item.mode==='raid'`）でもreading itemが上と同じ経路で出題可能になるが、`ReadingScreen`にはDrillScreenの「レイド」ヘッダ表示に相当する分岐が無い（見た目のみの差、レイドダメージ計算・記録は通常どおり動く）。3.3節・3.5節の記載範囲外のため本タスクでは対応しない。
- 検証: `engine/quickPack.test.ts`・`engine/curriculum.test.ts`に読解配分・Part7複数除外の回帰テストを追加、`screens/DrillScreen.test.tsx`・`screens/ReadingScreen.test.tsx`に画面切替のテストを追加。ルート`npm run lint`・`npm run format:check`・`npm run build`・`npm test`（全ワークスペース）で確認（詳細・既知のCPU競合flakeはSTATUS.md参照）。

**T-106 🟢 学習ロジック接続**
- 正本参照: 3.2節・3.4節、engine/rating.ts・tagStats.ts・keyVocab.ts・noEarphoneFilter.ts
- 変更: 読解の各subQuestionをRレートに反映、読解解法タグをtagStatsへ、誤答→keyVocab循環へ接続、noEarphoneFilterの実機能確認
- 完了条件: 読解正誤でRレートが動く（2/3ルールを使わないことをテストで担保）。読解タグが弱点判定に乗る。noEarphoneFilterがtext_passageへ差し替わるテスト。全通過。

**T-107 🟡 CLI生成＋初期パック（Part6・Part7単一）**
- 正本参照: 3.6節、04の5節、11（レビュー運用手順）
- 変更: `packages/cli` に読解生成、整合検証、`content/packs/` にPart6・Part7単一の初期パック、manifest更新
- 完了条件: 生成物が T-103 の validate と整合検証を通る。**人手レビューゲートを経たパックのみ配信**（レビュー未実施分は配信しない）。🟡 人手レビューは人間タスク（4節末の H-R1 参照）。

### フェーズR-2（じっくり読解: Part7複数パッセージ）

**T-108 🟢 複数パッセージUIと「じっくり読解」モード**
- 正本参照: 3.5節、ADR 0006 判断2
- 変更: ReadingScreenの複数パッセージ対応（タブ切替・相互参照）、「じっくり読解」モードの導線（HomeScreen等）
- 完了条件: 2〜3文書の切替・相互参照問題を解ける。「じっくり読解」モードから複数パッセージセットを起動できる。単体テスト全通過。

**T-109 🟡 Part7複数の生成・レビュー・P3配分**
- 正本参照: 3.3節・3.6節、03の1.2節（P3）
- 変更: CLIの複数パッセージ生成、`content/packs/` に複数パッセージパック、P3配分反映
- 完了条件: 複数パッセージパックがvalidate・整合検証を通る。人手レビュー済みのみ配信。P3で複数パッセージが「じっくり読解」に出る。全通過。

**T-110 🟢 ダッシュボードにRCペース指標**
- 正本参照: 3.5節、DashboardScreen・engine/forecast.ts
- 変更: 読解の解答時間（responseMs）から速読ペース指標を集計・表示
- 完了条件: RCの1問あたり平均解答時間・目標ペース（約1分/問）との差をダッシュボードに表示。単体テスト全通過。

### 人間タスク

- **H-R1 🔴 読解コンテンツの人手レビュー**: T-107・T-109で生成したパックの人手レビュー（事実整合・自然さ・ディストラクタ品質）。AIクロスレビューでは代替しない（ADR 0006 判断5）。
- **H-R2 🔴 実機での読解UX確認**: 複数パッセージのタブ切替・相互参照が実機（iOS/Android・片手/着席）で成立するか。単体テストで代替しきれない。

## 5. 完了ゲート

- フェーズR-1完了 = T-103〜T-107完了 かつ H-R1（R-1分レビュー）済み。**この時点で通勤セッション内でPart6・Part7単一が解け、Rレートに反映される**。
- フェーズR-2完了 = T-108〜T-110完了 かつ H-R1（R-2分）・H-R2済み。**この時点でTOEIC L&R全パート（Part1除く）がアプリ内で練習可能になる**。
- 全完了時に 02（読解の実装状況）・03（配分の実体化）・STATUS.md を更新し、docsと実装の齟齬を解消する。

## 6. 判断事項（発起人が推奨値で承認済み・2026-07-21）

以下は3節で暫定値を置いていたが、発起人が推奨値で一括承認した。数値はドッグフード実測で調整してよい（暫定扱い）。

- **J-51（承認済み）**: 初期在庫量は Part6 30セット（120問）／Part7単一 40セット（約120問）／Part7複数 15セット（75問）。人手レビュー体制（H-R1）は発起人が担う前提で進める。量は実測で調整可。
- **J-52（承認済み）**: 「じっくり読解」モードはHomeScreenのモードグリッドに独立入口として置き、通勤クエスト（3/7/15分チップ）とは視覚的に分離する。着席・自宅想定である旨をラベルで示す。
- **J-53（承認済み）**: 本R計画はロードマップ06の独立枠として扱う（M1/M2の積み残し扱いにしない）。06の更新はドキュメント整備タスクとして完了ゲートで行う（実装非ブロッキング）。
